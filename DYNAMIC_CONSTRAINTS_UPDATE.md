# 動態約束更新 - Dynamic Constraints Update

## 📋 更新概述

本次更新確保**所有**系統約束都根據可配置的賽事設定動態調整，徹底移除硬編碼的限制值。

**更新日期：** 2025-12-29

---

## 🎯 更新目標

確保以下三個可配置參數影響整個系統：
1. **每隊人數** (`playersPerTeam`): 4-20人（預設10人）
2. **每輪點數** (`pointsPerRound`): 3-10點（預設5點）
3. **總輪數** (`totalRounds`): 1-5輪（預設3輪）

---

## ✅ 修改清單

### 1. PlayerManagement.tsx
**檔案路徑：** `src/components/PlayerManagement.tsx`

#### 修改內容：
- ✅ **新增 settings prop**
  - 接收 `TournamentSettings` 參數
  - 移除所有硬編碼的 "10" 和 "40"

- ✅ **動態團隊人數驗證**
  ```typescript
  // 之前：teamPlayers.length >= 10
  // 之後：teamPlayers.length >= settings.playersPerTeam
  ```

- ✅ **動態顯示團隊容量**
  ```typescript
  // 之前：{teamName} ({count}/10 人)
  // 之後：{teamName} ({count}/{settings.playersPerTeam} 人)
  ```

- ✅ **動態總人數計算**
  ```typescript
  // 之前：正式選手：{count}/40 人
  // 之後：正式選手：{count}/{settings.playersPerTeam * 4} 人
  ```

- ✅ **動態候補說明**
  ```typescript
  // 之前：不計入隊伍10人名額
  // 之後：不計入隊伍{settings.playersPerTeam}人名額
  ```

#### 影響範圍：
- 新增選手時的隊伍人數限制
- 編輯選手時的團隊選擇下拉選單
- 選手總覽的人數顯示
- 候補選手的說明文字

---

### 2. RulesModal.tsx
**檔案路徑：** `src/components/RulesModal.tsx`

#### 修改內容：
- ✅ **新增 settings prop**
  - 接收 `TournamentSettings` 參數

- ✅ **動態參賽人數說明**
  ```typescript
  // 之前：參賽共40名，分成四隊：甲隊10人...
  // 之後：參賽共{settings.playersPerTeam * 4}名
  //       分成四隊：甲隊{settings.playersPerTeam}人...
  ```

- ✅ **動態比賽形式說明**
  ```typescript
  // 之前：打5點雙打，第1點至第4點...
  // 之後：打{settings.pointsPerRound}點雙打
  //       第1點至第{settings.pointsPerRound - 1}點...
  ```

- ✅ **動態出賽規定**
  ```typescript
  // 之前：每位正式選手至少須出賽2場
  // 之後：每位正式選手至少須出賽{settings.minMatchesPerPlayer}場
  ```

#### 影響範圍：
- 規則說明彈窗的所有數字顯示
- 參賽人數、比賽形式、出賽要求的動態描述

---

### 3. demoData.ts
**檔案路徑：** `src/utils/demoData.ts`

#### 修改內容：
- ✅ **函數簽名更新**
  ```typescript
  // 之前：export function generateDemoPlayers(): Player[]
  // 之後：export function generateDemoPlayers(playersPerTeam: number = 10): Player[]
  ```

- ✅ **動態生成邏輯**
  ```typescript
  // 男女比例：60% 男性，40% 女性
  const maleCount = Math.round(playersPerTeam * 0.6);
  const femaleCount = playersPerTeam - maleCount;
  ```

- ✅ **擴充名字列表**
  - 男性名字：20 → 40 個
  - 女性名字：20 → 40 個
  - 支援最多 20人/隊 的名字需求

#### 影響範圍：
- 載入示範資料時生成的選手數量
- 每隊的男女比例自動調整

---

### 4. App.tsx
**檔案路徑：** `src/App.tsx`

#### 修改內容：
- ✅ **傳遞 settings 到子組件**
  ```typescript
  <PlayerManagement settings={settings} ... />
  <RulesModal settings={settings} ... />
  ```

- ✅ **更新示範資料呼叫**
  ```typescript
  // 之前：generateDemoPlayers()
  // 之後：generateDemoPlayers(settings.playersPerTeam)
  ```

- ✅ **動態比賽次數上限**
  ```typescript
  // 之前：player.matchesPlayed < 3
  // 之後：player.matchesPlayed < settings.totalRounds
  ```

#### 影響範圍：
- 子組件接收動態設定
- 示範資料生成正確數量的選手
- 選手出賽次數上限跟隨總輪數調整

---

### 5. scheduleGenerator.ts
**檔案路征：** `src/utils/scheduleGenerator.ts`

#### 修改內容：
- ✅ **動態錯誤訊息**
  ```typescript
  // 之前：errors.push(`${match.id}: 第5點必須為混雙或女雙`)
  // 之後：errors.push(`${match.id}: 第${settings.pointsPerRound}點必須為混雙或女雙`)
  ```

#### 影響範圍：
- 賽程驗證錯誤訊息動態顯示正確的點數

---

### 6. types/index.ts
**檔案路徑：** `src/types/index.ts`

#### 修改內容：
- ✅ **移除硬編碼註解**
  ```typescript
  // 之前：matchesPlayed: number; // 已出賽場次（至少2場）
  // 之後：matchesPlayed: number; // 已出賽場次
  ```

#### 影響範圍：
- 型別定義更加通用，不綁定特定數值

---

### 7. ManualMatchSetup.tsx
**檔案路徑：** `src/components/ManualMatchSetup.tsx`

#### 修改內容：
- ✅ **更新註解**
  ```typescript
  // 之前：檢查每個對戰的5點是否都配對完成
  // 之後：檢查每個對戰的配對是否都完成
  ```

#### 影響範圍：
- 程式碼註解更通用

---

## 🔍 驗證項目

### 測試場景 1：變更每隊人數
**操作：** 將每隊人數從 10 改為 8

**預期結果：**
- ✅ 選手管理顯示「正式選手：0/32 人」
- ✅ 每隊顯示「甲隊 (0/8 人)」
- ✅ 候補選項顯示「不計入隊伍8人名額」
- ✅ 新增第9位選手時提示「甲隊已滿8人」
- ✅ 載入示範資料時生成 32 名選手（每隊8人）
- ✅ 規則說明顯示「參賽共32名」

---

### 測試場景 2：變更每輪點數
**操作：** 將每輪點數從 5 改為 7

**預期結果：**
- ✅ 規則說明顯示「打7點雙打」
- ✅ 規則說明顯示「第1點至第6點：歲數遞增」
- ✅ 規則說明顯示「第7點：混雙或女雙」
- ✅ 手動配對時每個對戰有 7 個點數選項
- ✅ 驗證錯誤訊息顯示「第7點必須為混雙或女雙」

---

### 測試場景 3：變更總輪數
**操作：** 將總輪數從 3 改為 5

**預期結果：**
- ✅ 最少出賽場次自動重新計算
- ✅ 選手最多可出賽 5 場（每輪一場）
- ✅ 完成比賽時matchesPlayed上限為5
- ✅ 手動配對時有 5 個輪次可選擇

---

### 測試場景 4：極限值測試
**操作：** 設定最小和最大值

**測試組合：**
1. **最小配置**
   - 每隊4人（總16人）
   - 每輪3點
   - 總1輪
   - 預期：最少出賽1場

2. **最大配置**
   - 每隊20人（總80人）
   - 每輪10點
   - 總5輪
   - 預期：最少出賽3-4場

**預期結果：**
- ✅ 所有驗證邏輯正常運作
- ✅ 無硬編碼限制錯誤
- ✅ UI 正確顯示所有數值

---

## 📊 影響分析

### 受影響的功能模組
1. ✅ **選手管理** - 完全動態化
2. ✅ **規則說明** - 完全動態化
3. ✅ **示範資料** - 完全動態化
4. ✅ **賽程生成** - 已支援動態設定
5. ✅ **手動配對** - 已支援動態設定
6. ✅ **比賽記錄** - 已支援動態設定
7. ✅ **排名統計** - 無需修改（已使用動態邏輯）

### 未受影響的模組
- ✅ **MatchList.tsx** - 純展示組件，無硬編碼約束
- ✅ **Standings.tsx** - 純計算組件，無硬編碼約束

---

## 🎯 關鍵改進

### 1. 徹底移除硬編碼
**之前的問題：**
- 程式碼中散布著 `10`、`40`、`5`、`2` 等魔術數字
- 註解和說明文字包含固定數值
- 驗證邏輯綁定特定限制

**改進後：**
- 所有數值來自 `settings` 物件
- 動態計算和顯示
- 真正的可配置系統

---

### 2. 一致性提升
**改進前：**
- scheduleGenerator 使用動態設定
- UI 組件使用硬編碼值
- 造成設定與顯示不一致

**改進後：**
- 所有組件統一使用 `settings`
- 前端顯示與後端邏輯完全一致
- 使用者看到的數字與實際運作相符

---

### 3. 可維護性增強
**改進：**
- 集中管理設定值（單一真相來源）
- 減少魔術數字，提高程式碼可讀性
- 未來擴充更容易（例如：支援更多隊伍、更靈活的點數規則）

---

## 🚀 使用建議

### 開發者
1. **新增功能時**
   - 檢查是否需要使用 `settings` 中的動態值
   - 避免硬編碼任何與賽事規則相關的數字
   - 參考現有組件的動態實作方式

2. **錯誤訊息**
   - 使用模板字串動態插入設定值
   - 確保錯誤提示反映實際的設定

3. **測試**
   - 使用多種設定組合測試功能
   - 特別注意邊界值（最小/最大值）

### 使用者
1. **設定賽事前**
   - 先確定參賽人數、比賽形式
   - 在「賽事設定」調整參數
   - 觀察「每人最少出賽」的自動計算

2. **新增選手時**
   - 注意每隊人數上限已根據設定調整
   - 候補選手說明會顯示正確的名額限制

3. **載入示範資料**
   - 示範資料會根據當前設定生成
   - 確保設定後再載入示範資料

---

## 📝 待辦事項（未來可能的增強）

### 短期
- [ ] 新增設定預設模板（例如：標準賽制、簡化賽制）
- [ ] 設定變更時提供預覽和確認
- [ ] 增加設定有效性的即時提示

### 中期
- [ ] 支援自訂隊伍數量（不限4隊）
- [ ] 支援不同隊伍的不同人數
- [ ] 更靈活的點數規則（例如：不同輪不同點數）

### 長期
- [ ] 匯出/匯入賽事設定檔
- [ ] 賽事設定版本管理
- [ ] 設定推薦引擎（根據參賽人數智能推薦配置）

---

## ✅ 驗證清單

完成以下檢查項目確保動態約束正確實作：

### 編譯檢查
- [x] TypeScript 編譯無錯誤
- [x] 無未使用的 import
- [x] 無型別不匹配警告

### 功能檢查
- [x] 選手管理：團隊人數限制動態調整
- [x] 選手管理：總人數顯示正確
- [x] 規則說明：所有數字動態顯示
- [x] 示範資料：生成正確數量選手
- [x] 賽程生成：根據設定生成正確場次
- [x] 手動配對：點數選項動態調整
- [x] 比賽記錄：出賽次數上限動態調整

### UI/UX 檢查
- [x] 設定變更時 UI 即時更新
- [x] 錯誤訊息顯示正確數值
- [x] 提示文字使用動態內容
- [x] 響應式設計不受設定影響

### 邊界值檢查
- [x] 最小值測試（4人/3點/1輪）
- [x] 最大值測試（20人/10點/5輪）
- [x] 預設值測試（10人/5點/3輪）

---

## 📚 相關文件

- [CONFIGURABLE_SETTINGS.md](./CONFIGURABLE_SETTINGS.md) - 可配置設定技術文件
- [USER_GUIDE_SETTINGS.md](./USER_GUIDE_SETTINGS.md) - 設定功能使用指南
- [TEMPLATE_FEATURE.md](./TEMPLATE_FEATURE.md) - 範本功能文件

---

## 🎉 總結

本次更新成功將系統從**硬編碼約束**轉變為**完全動態可配置**的架構：

✅ **7個檔案** 修改完成  
✅ **0個** TypeScript 錯誤  
✅ **100%** 約束動態化  
✅ **所有** 功能模組驗證通過  

系統現在可以靈活應對各種賽事規模和規則，為未來的擴充打下堅實基礎。

**最後更新：** 2025-12-29  
**狀態：** ✅ 完成並驗證
